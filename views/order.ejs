<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Order Confirmation</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 20px;
    }

    h2 {
      text-align: center;
    }

    table {
      margin: 20px auto;
      border-collapse: collapse;
      width: 80%;
    }

    table th, table td {
      border: 1px solid #ddd;
      padding: 10px;
      text-align: center;
    }

    table th {
      background-color: #f4f4f4;
      font-weight: bold;
    }

    .total-amount {
      font-size: 1.2em;
      font-weight: bold;
      text-align: center;
      margin-top: 20px;
    }

    a {
      display: block;
      text-align: center;
      margin-top: 20px;
      font-size: 1.1em;
      color: #007bff;
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <h2>Order Confirmation</h2>
  <p style="text-align: center;">Thank you for your order!</p>
  <p><strong>Order ID:</strong> <%= order.formattedOrderNumber %></p>
  <table>
    <thead>
      <tr>
        <th>Product Name</th>
        <th>Price</th>
        <th>Quantity</th>
        <th>Total</th>
      </tr>
    </thead>
    <tbody>
      <% order.products.forEach(item => { %>
        <tr>
          <td><%= item.name %></td>
          <td>₩<%= item.price.toLocaleString() %></td>
          <td><%= item.quantity %></td>
          <td>₩<%= (item.price * item.quantity).toLocaleString() %></td>
        </tr>
      <% }); %>
    </tbody>
  </table>
  <div class="total-amount">
    Total Amount: ₩<%= order.totalAmount.toLocaleString() %>
  </div>
  <a href="/orders/history">View Order History</a>
</body>
</html>
